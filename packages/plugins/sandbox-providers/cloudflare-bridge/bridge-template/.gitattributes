# Shell scripts copied into the Linux container image must keep LF line
# endings — a CRLF shebang makes the container fail with exit 127
# ("/bin/sh\r: No such file or directory"), regardless of the host OS.
*.sh text eol=lf
tailscale-up text eol=lf
Dockerfile text eol=lf
